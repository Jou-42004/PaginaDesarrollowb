export enum EstadoPedido {
  PENDIENTE = "pendiente",
  PREPARACION = "preparacion",
  ENVIADO = "enviado",
  ENTREGADO = "entregado",
  CANCELADO = "cancelado"
}

export enum EstadoPago {
  PENDIENTE = "pendiente",
  APROBADO = "aprobado",
  RECHAZADO = "rechazado"
}

export enum TipoDocumento {
  BOLETA = "boleta",
  FACTURA = "factura"
}
